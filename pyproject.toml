[project]
name = "stats-service"
version = "1.0.0"
description = "Batingeers Stats Service — Estadísticas académicas (R20, R21)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
